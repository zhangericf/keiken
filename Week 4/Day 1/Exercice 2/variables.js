// Working with variables

let admin;
let name;

name = "John";
admin = name;

alert(admin);

// Giving the right name

let ourPlanetName = "Earth";
let currentVisitorName = "Eric";

// Uppercase const?

// capital-named constants are only used as aliases for “hard-coded” values.
const BIRTHDAY = '18.04.1982'; //hard value

const age = someCode(BIRTHDAY); //calculated in run-time, so lower case